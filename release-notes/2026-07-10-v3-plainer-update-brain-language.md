# 2026-07-10 — v3: plainer `/update-brain` language

`/update-brain` now explains factory updates in everyday words (changes, new version) instead of git jargon. `migrations/v1.md` also pushes harder for the submodule layout. No brand-side layout work — `migrations/v3.md` is a no-op.

Cut the `v3` tag after merge.
