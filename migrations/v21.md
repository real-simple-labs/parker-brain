# v21 — optional usage logging

Nothing to do outside the executable-layer re-sync: it delivers the collector,
both runtime hook configurations, and the save export step. Leave
`parker_config.json` unchanged; a missing `usage_logging` option means off.
Do not create `.usage/` or enable logging during migration. Report retained
team overrides that prevent collection, and approve changed hook commands in
the runtime before relying on enabled logging. Record `v21`.
