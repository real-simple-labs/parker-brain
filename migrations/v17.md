# v17 — runtime compatibility fixes

## Qualify the brand's scheduling instructions

In the brand-authored root `CLAUDE.md`, find the routines / schedule-setup section.
Add this capability guard before any offer to register schedules, unless an
equivalent rule already exists. Preserve the team's other instructions:

> First check the runtime's scheduling capability. In Codex, Claude cloud
> scheduling is unavailable: the routines run on demand and scheduling is
> explicitly deferred. Do not offer to register them from this session. This
> does not block a completed build. Only describe background runs as active after
> observing their registrations. Offer setup only where the scheduler is available.

In that same section, qualify any claim that routines keep the brain fresh
"without being asked": they run on demand or through a registered schedule.
Keep the setup instructions for scheduling-capable sessions.

The normal executable-layer re-sync delivers all hook, permission-profile,
review, and routine-skill changes. Do not copy or patch those files here. Record
`v17` after the runner completes the migration and re-sync.
