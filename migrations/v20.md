# v20 — the mount is a check first

**Applies to:** brains pinned at `v19` or earlier. No-op on `v20+`.

**What changes:** Parker Desktop now attaches the method mount itself when it creates a brain, so the onboarding runner checks for an existing mount before adding one; `system/brain-sync.md` and `system/codex-support.md` record the app-side change. No brand-repo layout work.

**Nothing to do on the brand side.** The runner and the contracts live in the mount. Record migrations through `v20` and set `parker_brain_version` to `v20` as usual.
