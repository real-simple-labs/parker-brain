# v23 — the brain scaffold

Nothing to do; the pin bump's re-sync delivers everything. Record `v23`.

The scaffold (`scripts/scaffold-brain.py`, the `.scaffolded` marker, the seed templates) only sets up new brains; an already-built brain has no marker and reads as built, which is right. The copied files that changed (`session-start.py`, `pull-log.py`, `get-started`, `update-brain`, `set-up-brain`, `schedules/README.md`) travel in the bundle. `pull-log.py` now also deletes the marker when a build reports its first finished phase after Phase 0; on a built brain there's no marker, so it does nothing new. If this brain's copies of the routine skills or schedules had the brand name typed into them by an older build, `/update-brain` lists them as team-edited and leaves them alone; that's expected, and nothing here changes it.
