# v19 — the method mount asks for approval, never copies

**Applies to:** brains pinned at `v18` or earlier. No-op on `v19+`.

**What changes:** the onboarding runner and `/set-up-brain` now treat the method mount (the build's `git submodule add` of the public factory) as the sanctioned exception to Parker Desktop's workspace guide, ask for the harness's approval when one is needed, and never copy the factory in as a substitute; the build verification fails a copied `parker-system/`. `system/brain-sync.md` and `system/codex-support.md` carry the contract. No brand-repo layout work.

**Nothing to do on the brand side.** The runner and the contracts live in the mount, and the `set-up-brain` skill rides the pin bump's re-sync of the executable layer. Record migrations through `v19` and set `parker_brain_version` to `v19` as usual.
