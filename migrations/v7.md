# v7 — reliable hook startup and factory tag synchronization

**Applies to:** brains pinned at `v6` or earlier (after prior migrations). No-op on `v7+`.

**What changes:** the routine hooks now fail open when their scripts are unavailable, and `/update-brain` synchronizes factory tag refs before it looks for a newer release. It replaces moved tags and removes tags deleted from the factory, so an old local tag cannot block or distort the comparison. No brand-repo layout work is needed.

**Nothing to do on the brand side.** The pin bump's normal re-sync of the copied executable layer picks up the updated `.claude/` hook registration and `/update-brain` skill. Record migrations through `v7` and set `parker_brain_version` to `v7` as usual.
