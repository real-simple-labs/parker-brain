# v13 — main-only git discipline and onboarding existence triage

Nothing to do; the pin bump's re-sync delivers everything (the strengthened `/save-brain` rule and README summaries ride the copied bundle; the onboarding and template changes only shape new builds). Record `v13`.
