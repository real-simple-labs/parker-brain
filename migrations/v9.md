# v9 — creative-skill review-gate wording

**Applies to:** brains pinned at `v8` or earlier (after prior migrations). No-op on `v9+`.

**What changes:** the review-gate instructions in five creative skills — `scriptwriting`, `hooks`, `headlines`, `iterations`, `ai-ad-generation` — were reworded so the two review agents read as a step the agent runs itself, not one that happens automatically. No brand-repo layout work.

**Nothing to do on the brand side.** The change lives entirely inside the copied bundle, so the pin bump's re-sync of the executable layer (`scripts/sync-executable-layer.py`) picks up the five skills. Record migrations through `v9` and set `parker_brain_version` to `v9` as usual.
