# v3 — plainer update-brain language

**Applies to:** brains pinned at `v2` or earlier (after prior migrations). No-op on `v3+`.

**What changes:** `/update-brain` talks about updates in plain language (no git jargon for the user). No brand-repo layout work.

**Nothing to do on the brand side.** Pin bump re-syncs `.claude/skills/update-brain/`. Record migrations through `v3` and set `parker_brain_version` to `v3` as usual.
